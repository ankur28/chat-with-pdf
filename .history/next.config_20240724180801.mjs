/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol
            }
        ]
        domains: ['imgur.com'],
      },
};

export default nextConfig;
